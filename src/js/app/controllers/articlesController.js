/*angular.module('ArticlesController', ['ui.router'])
.config(function($stateProvider){
	$stateProvider
		.state("articles", {
			url: "/articles",
			templateUrl: "partials/articles.html",
			controller: 'ArticlesController',
			data: {
				pageTitle: "Articles"
			}
		});
});

app.controller( 'ArticlesController', ['$scope', '$location', function ( $scope, $location ) {
	console.log("in Articles");
}]);*/