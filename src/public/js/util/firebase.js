// For details see https://firebase.google.com/docs/web/setup/#from-the-cdn

export const firebaseConfig = {
  //,,,
};

// Initialize Firebase
firebase.initializeApp(firebaseConfig);
firebase.analytics();
